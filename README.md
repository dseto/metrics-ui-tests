# metrics-ui-tests-skeleton

Conteúdo gerado:

- `delta-pack/` — arquivos de delta pack para o spec deck
- `specs/` — novos specs pro spec deck (ui-automation)
- `tests/ui/` — skeleton de projeto .NET (Selenium + xUnit + Reqnroll)
